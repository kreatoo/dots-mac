# Per-user feature toggles and options for kreato
{
  programs = {
    zsh.enable = true;
    nushell.enable = true;
    starship.enable = true;
    nixvim.enable = true;
    neovide.enable = true;
  };
}
